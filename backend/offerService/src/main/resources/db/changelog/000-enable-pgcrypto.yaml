databaseChangeLog:
  - changeSet:
      id: enable-pgcrypto-extension
      author: trishita
      preConditions:
        - dbms:
            type: postgresql
      changes:
        - sql:
            sql: CREATE EXTENSION IF NOT EXISTS pgcrypto;
